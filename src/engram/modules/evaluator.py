# Flexible Distance-to-Goal (paper Section 5)
class DistanceEvaluator:
    def bind_goal(self, goal):
        self.tiling = [...]  # gamma-like distances
    def score(self, futures):
        return [dist for each]